using System.Linq;
using System.Collections.Generic;
using UnityEngine;

public class RoomInitializer : MonoBehaviour
{
//    public RoomData roomData;

//    void Awake()
//    {
//        if (roomData == null)
//        {
//            Debug.LogError($"[{name}] No se asignó RoomData.");
//            return;
//        }

//        List<GameObject> enemies = GetComponentsInChildren<EnemyBase>().Select(e => e.gameObject).ToList();
//        DoorController[] doors = GetComponentsInChildren<DoorController>();

//        roomData.enemies = enemies;
//        roomData.entryDoor = doors.FirstOrDefault(d => d.name.Contains("Entry"));
//        roomData.exitDoor = doors.FirstOrDefault(d => d.name.Contains("Exit"));

//        roomData.Initialize();
//    }
}
